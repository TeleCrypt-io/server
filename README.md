# TeleCrypt.io server configuration

Public runtime configuration for the TeleCrypt Matrix service. The repository contains the
versioned systemd, Quadlet and Salt source used to assemble a host; credentials and populated
environment files stay outside Git.

See [`LICENSE`](./LICENSE) for licensing and [`SECURITY.md`](./SECURITY.md) for private reports.
